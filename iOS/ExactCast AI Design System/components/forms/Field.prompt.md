One-line: 14px/600 label above the control, hint in 12.5px muted underneath — hints explain units, never repeat the label.

```jsx
<Field label="Hectares beregening" hint="Alleen het areaal dat je daadwerkelijk beregent"><Input/></Field>
```
