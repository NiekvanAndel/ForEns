One-line: stands in for the logo — "Exact" black weight, "Cast" medium, "AI" in the cool precipitation blue.

```jsx
<Wordmark pill />              {/* header, on navy */}
<Wordmark tone="light" size={26} />
```

- Never set the mark in green: green is a product accent (buttons, ticks, eyebrows) and reads as agriculture.
  On light it uses `--app-accent` (#2A628E); on dark, `--app-accent-dark` (#5FA3CE).
- Flagged: no logo file exists in this system yet. Do not redraw the AgroExact mark from memory.
