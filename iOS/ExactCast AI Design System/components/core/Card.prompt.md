One-line: every block of content on cream sits in one of these; cards float on shadow, never on a border.

```jsx
<Card><h3>Kan ik het land op?</h3><p>…</p></Card>
```
`alt` gives the flat cream-2 tile used *inside* a card (stats, gates). Radius is 16px on web, 24px in the app.
