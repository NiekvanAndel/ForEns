import * as React from 'react';
/** Uppercase 13px/700 label with 0.14em tracking that opens a section. Green-dark on cream, green on navy. */
export interface EyebrowProps { children?: React.ReactNode; tone?: 'green' | 'light'; style?: React.CSSProperties; }
export function Eyebrow(props: EyebrowProps): JSX.Element;
