One-line: the honest comparison; an em-dash in --ink-disabled means "doesn't do this".

```jsx
<ComparisonTable columns={['Gratis weerapp','Buienradar','ExactCast AI']} rows={[{label:'Bron',cells:['Model','Radar','Radar + jouw station']}]}/>
```
