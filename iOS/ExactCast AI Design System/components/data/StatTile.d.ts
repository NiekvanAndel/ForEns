import * as React from 'react';
/** Flat cream-2 tile inside a card: 27px green-dark number over a 12.5px muted caption. */
export interface StatTileProps { value?: React.ReactNode; label?: React.ReactNode; style?: React.CSSProperties; }
export function StatTile(props: StatTileProps): JSX.Element;
