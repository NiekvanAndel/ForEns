One-line: colour never carries meaning alone — always pair the dot with a word.

```jsx
<StatusDot status="light"/> Lichte regen mogelijk
```
Cream ("dry") carries a 1.5px inset hairline so it stays visible on white.
