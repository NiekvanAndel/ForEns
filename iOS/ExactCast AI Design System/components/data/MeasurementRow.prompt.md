One-line: the "which of my locations needs attention" row.

```jsx
<MeasurementRow status="heavy" name="Thuis" meta="4,2 mm · 35 min" advice="Nu regen"/>
```
Advice text uses the ink variants (--status-dry-ink / --status-light-ink / --status-heavy-ink), never the raw fill, so it stays legible on white.
