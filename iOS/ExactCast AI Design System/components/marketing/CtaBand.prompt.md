One-line: the one place green becomes a background instead of an accent.

```jsx
<CtaBand title="Zie de eerste bui aankomen" body="…" bullets={['Gratis proefperiode']}><Button variant="white">Download</Button></CtaBand>
```
Never two CTA bands on one page.
