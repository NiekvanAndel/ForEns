One-line: switches between the user's saved locations at the top of an app screen.

```jsx
<PillTabs items={['Thuis','Tuin achter','Volkspark']} active={0} onChange={setLoc}/>
```
The last pill is deliberately cut off at the screen edge to signal scrollability.
