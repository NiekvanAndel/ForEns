# assets

**Empty on purpose.** The supplied sources contained no logo, illustration or photograph files —
only an HTML page that hot-links images from `agroexact.com` (`/images/Agroexact-logo-Donkerblauw-1.png`,
`/images/rijden.jpg`) and one iOS screenshot.

Nothing was drawn or reconstructed from memory. Until real files land here:

- the brand mark is set in type — see `components/brand/Wordmark.jsx`;
- icons come from the Lucide CDN — see `components/brand/Icon.jsx`;
- photography is left out rather than substituted with stock.

**A `favicon.svg` was uploaded on 30 Aug but could not be decoded** — the file appears to be corrupt or
not valid SVG markup. Nothing was inferred from it. Please re-upload it (or send the source logo) and it
will become the wordmark specimen.

**To fill this in, drop the real files in here:** logo (SVG + PNG, light and dark), app icon,
App Store screenshots, and two or three landscape/weather photographs. Then update
`Wordmark.jsx` and the `Brand` cards in `guidelines/`.
